info:
  name: getProfilePicture
  type: http
  seq: 3
  tags:
    - file-upload-controller

http:
  method: GET
  url: "{{baseUrl}}/files/profile/:userId"
  params:
    - name: userId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/files/profile/:userId"
      method: GET
      params:
        - name: userId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: ""
