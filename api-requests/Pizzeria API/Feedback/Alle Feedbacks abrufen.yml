info:
  name: Alle Feedbacks abrufen
  type: http
  seq: 1
  tags:
    - Feedback

http:
  method: GET
  url: "{{baseUrl}}/api/feedback"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/feedback"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          [
            {
              "id": "",
              "customerEmail": "",
              "createdAt": "",
              "details": {}
            }
          ]
