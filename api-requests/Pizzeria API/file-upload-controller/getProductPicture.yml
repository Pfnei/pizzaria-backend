info:
  name: getProductPicture
  type: http
  seq: 4
  tags:
    - file-upload-controller

http:
  method: GET
  url: "{{baseUrl}}/files/product/:productId"
  params:
    - name: productId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/files/product/:productId"
      method: GET
      params:
        - name: productId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: ""
