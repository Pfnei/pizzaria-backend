info:
  name: uploadProductImage
  type: http
  seq: 2
  tags:
    - file-upload-controller

http:
  method: POST
  url: "{{baseUrl}}/files/productpicture/:productId"
  params:
    - name: productId
      value: ""
      type: path
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value: []
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/files/productpicture/:productId"
      method: POST
      params:
        - name: productId
          value: ""
          type: path
      body:
        type: multipart-form
        data:
          - name: file
            type: file
            value: []
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: ""
